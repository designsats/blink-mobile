# Claude Code Project Notes

## APK Builds
- After building an APK, always copy it to `~/Documents/APKs/` and zip it
- Naming convention: `blink-personal-{buildType}-arm64.apk` (e.g. `blink-personal-release-arm64.apk`)
- Zip with the same base name (e.g. `blink-personal-release-arm64.zip`)
